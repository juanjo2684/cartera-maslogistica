# ============================================================
# Dependencias del proyecto — Cartera +logística (MVP)
# ============================================================
# Instalación:
#   pip install -r requirements.txt
#
# Se fijan versiones mínimas, no exactas, para tolerar parches
# menores. Si se requiere reproducibilidad estricta, ejecutar
# `pip freeze > requirements.lock.txt` al final del desarrollo.
# ============================================================

# Manipulación de datos
pandas>=2.0
numpy>=1.24

# Lectura/escritura de Excel
openpyxl>=3.1

# Dashboard del analista
streamlit>=1.32
